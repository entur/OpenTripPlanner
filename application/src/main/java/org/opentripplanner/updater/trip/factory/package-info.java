/**
 * Resolves a command against the transit model into a validated change - or rejects it. Factory =
 * command to change; the resolvers in {@code org.opentripplanner.updater.trip.resolver} cover
 * reference to entity.
 */
package org.opentripplanner.updater.trip.factory;
