/**
 * Resolves a command against the transit model into a validated change - or rejects it. Factory =
 * command to change; the resolvers in {@code org.opentripplanner.ext.updater.trip.unified.resolver} cover
 * reference to entity.
 */
package org.opentripplanner.ext.updater.trip.unified.factory;
